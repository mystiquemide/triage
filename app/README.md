# Triage App

Vue/Vite frontend for Triage, a GenLayer-powered security bounty platform.

## Development

```bash
npm install
cp ../.env.example .env
npm run dev
```

## Build

```bash
npm run build
```

The production build is emitted to `dist/`.
