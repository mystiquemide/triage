{
  "name": "triage",
  "version": "0.1.0",
  "description": "AI-assisted bug bounty triage powered by GenLayer intelligent contracts.",
  "type": "module",
  "scripts": {
    "build": "npm --prefix app run build",
    "dev": "npm --prefix app run dev",
    "preview": "npm --prefix app run preview",
    "test:contracts": "./scripts/check-security-bounty.sh"
  },
  "devDependencies": {
    "genlayer-js": "^0.9.0"
  }
}
